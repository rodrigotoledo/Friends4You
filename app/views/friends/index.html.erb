<% content_for :title do%>
Amigos
<% end %>
<article class="module width_full">
  <header><h3>Amigos ordenados pela distância</h3></header>
  <div class="module_content">
    <p>Veja abaixo a lista dos amigos que estão próximos a você</p>
    <ul>
      <% current_user.all_friends.each do |friend|%>
      <li><%=friend.distance.round(3)%> km - <%=link_to friend.name, friend_path(friend)%></li>
      <% end %>
    </ul>
  </div>
</article>